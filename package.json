{
  "name": "watchapi",
  "version": "1.0.0",
  "description": "WatchAPI monorepo - REST API client and CLI tools",
  "private": true,
  "scripts": {
    "generate:api": "bash scripts/generate-api.sh",
    "check-types": "pnpm -r --parallel run check-types",
    "lint": "pnpm -r --parallel run lint",
    "package": "pnpm -r run package",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "vscode:package": "pnpm --filter watchapi-client run vscode:package",
    "vscode:publish": "pnpm --filter watchapi-client run vscode:publish"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.18.3",
  "devDependencies": {
    "openapi-typescript": "^7.10.1"
  },
  "dependencies": {}
}